<section class="{{ $name }}-wrapper section-wrapper">
    <div class="container">
        <div class="{{ $name }}-desc">
            <span>{{ $subtitle }}</span>
            <h2>{{ $title }}</h2>
            <p>{{ $desc }}</p>
            <div class="{{ $name }}-cta">
                {{ $btn }}
            </div>
        </div>
        <div class="{{ $name }}-img">
            {{ $img }}
        </div>
    </div>
</section>